diff --git a/dist/fs/index.js b/dist/fs/index.js
index cff44fac5414036231f252758f13c3161e9d8e9b..33a7a27e1bb522a50b9f84aaddf3c6f4e2a39a81 100644
--- a/dist/fs/index.js
+++ b/dist/fs/index.js
@@ -11,7 +11,8 @@ export const getFileListSync = (dirName, og = dirName) => {
             files.push(...getFileListSync(fullPath, og));
         }
         else {
-            files.push(path.relative(og, fullPath));
+            // Fix Windows path separators
+            files.push(path.relative(og, fullPath).replace(/\\/g, '/'));
         }
     }
     return files;
@@ -23,14 +24,15 @@ export const getFileListWithDirectories = (dirName, og = dirName) => {
     for (const item of items) {
         const fullPath = path.join(dirName, item.name);
         if (item.isDirectory()) {
-            const relativeDir = path.relative(og, fullPath);
+            const relativeDir = path.relative(og, fullPath).replace(/\\/g, '/');
             if (relativeDir !== '.git') {
                 files.push(relativeDir);
                 files.push(...getFileListWithDirectories(fullPath, og));
             }
         }
         else {
-            files.push(path.relative(og, fullPath));
+            // Fix Windows path separators
+            files.push(path.relative(og, fullPath).replace(/\\/g, '/'));
         }
     }
     return files;
@@ -47,7 +49,8 @@ export async function* getFileList(dirName, og = dirName, mintIgnore = []) {
             yield* getFileList(fullPath, og, mintIgnore);
         }
         else {
-            yield `/${relativePath}`;
+            // Fix Windows path separators
+            yield `/${relativePath.replace(/\\/g, '/')}`;
         }
     }
 }
